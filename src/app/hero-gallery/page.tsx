import HeroGalleryPage from "@/features/hero-gallery/components/hero-gallery-page";

export const metadata = {
  title: "Hero Gallery | Codegrid",
};

export default function Page() {
  return <HeroGalleryPage />;
}
